Create a side bar which will include all the relevant page names. For now these pages don't go anywhere.

- Create a dashboard page which will show static valuves for now. Add a row which will show total net worth, liquid cash, invested cash.
- There should be a graph which shows income vs expenses vs investments.
- There should be asset allocation graph which will show how much percentage you have.
- It should show all the connected wallets and how much money is stored in each
- It should show expense breakdown per category. and how much it has spent.
